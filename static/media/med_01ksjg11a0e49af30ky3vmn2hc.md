更好的方案：直接用 Cat Mate 的 Training Mode（训练模式）

进入训练模式的方法：取出一节电池，等至少 2 分钟，重新插入，等锁扣落下（约 5 秒），然后立即再次取出一节或全部电池，这样锁扣就会保持在落下状态（门常开），不会再弹起。 

要退出训练模式，只需重新安装所有电池即可。

训练模式下，锁扣靠物理位置保持落下，不需要芯片信号来维持开门状态，所以不存在持续扫描的耗电问题。

你的使用场景（老大老二不用戴项圈自由进出、老三不会用猫门所以常开也没问题）和训练模式的设计目的完全吻合，直接用这个就好。